This is a 3D chess game made as a school project. It uses OpenGL and SDL and is
written in C. It can be played normally or run a replay. To play a replay, just
run the program with a PGN file as a command line argument.
